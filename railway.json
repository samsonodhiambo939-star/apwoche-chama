{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "node server.js",
    "healthcheckPath": "/login",
    "healthcheckTimeout": 300, // triggering redeploy
    "restartPolicyType": "always"
  }
}